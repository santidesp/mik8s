apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: valkey-demo-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/santidesp/mik8s.git'
    targetRevision: HEAD
    path: infrastructure/instances/valkey-demo/manifests
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
